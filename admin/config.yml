backend:
  name: git-gateway
  branch: main

media_folder: "images"
public_folder: "/images"

collections:
  - name: "test"
    label: "Test"
    folder: "content/test"
    create: true
    fields:
      - { label: "Başlık", name: "title", widget: "string" }